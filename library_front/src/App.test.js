
test('Test app', () => {
  expect(1 + 1).toBe(2);
});

test('Test app', () => {
  expect(1 - 1).toBe(0);
});

test('Test app', () => {
  expect(2 + 2).toBe(4);
});

test('Test app', () => {
  expect(10 + 10).toBe(20);
});
