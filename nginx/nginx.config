events {}

http {
  server {
    listen 80;

    location /api {
      proxy_pass http://api:8000;
    }

    location / {
      root /app;
      index index.html;
      try_files $uri /index.html;
    }
  }
}
